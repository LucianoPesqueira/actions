name: Deploy to server

on: push

jobs:
  first-jobs:
    runs-on: ubuntu-latest

    steps:
      - name: Hola Mundo
        run: echo "Hola Mundo"
  
  second-jobs:
    if: ${{ github.ref == 'refs/heads/main' }}
    needs: first-jobs
    runs-on: ubuntu-latest

    steps:
      - name: Hola Mundo
        run: echo "Hola Mundo Segundo"