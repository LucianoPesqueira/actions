name: Deploy to server

on: push

jobs:
  first-jobs:
    runs-on: ubuntu-latest

    env:
      NAME: "CodigoFacilito"

    steps:
      - name: Hola Mundo
        run: echo "Hola Mundo $NAME"
  
  second-jobs:
    needs: first-jobs
    runs-on: ubuntu-latest

    steps:
      - name: Hola Mundo
        run: echo "Hola Mundo ${{ github.ref }}"