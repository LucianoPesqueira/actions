name: Deploy to server

on: push

jobs:
  first-jobs:
    runs-on: ubuntu-latest

    steps:
      - name: Hola Mundo
        run: echo "Hola Mundo"
  
  second-jobs:
    needs: first-jobs
    runs-on: ubuntu-latest

    steps:
      - name: Hola Mundo
        if: false
        run: echo "Hola Mundo ${{ github.ref }}"